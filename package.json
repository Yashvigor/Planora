{
    "name": "planora-root",
    "version": "1.0.0",
    "description": "Root package for running Planora Frontend and Backend concurrently",
    "scripts": {
        "install-all": "npm install && npm install --prefix Backend && npm install --prefix Frontend",
        "dev": "concurrently \"npm run dev --prefix Backend\" \"npm run dev --prefix Frontend\"",
        "start": "npm run dev"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    }
}